import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  searchContainer: {
    borderBottomWidth: 1,
    marginBottom: 25,
  },
  searchInput: {
    padding: 16,
    marginHorizontal: 30,
  },
});
